# 每日学习笔记

## 2026-05-10

### 今天学到的 Git 命令

- git init —— 初始化仓库
- git status —— 查看仓库状态
- git add —— 将文件添加到暂存区
- git commit —— 将暂存区内容提交到仓库

### 三个核心区域

1. 工作区（Working Tree）—— 你正在编辑的文件
2. 暂存区（Staging Area）—— 准备提交的快照
3. 仓库（Repository）—— 永久的历史记录

### 常用命令速查

| 命令 | 作用 |
|------|------|
| git status | 查看工作区、暂存区、仓库的状态 |
| git diff | 查看具体修改内容 |
| git log | 查看提交历史 |

### 今日心得

Git 的三个区域概念是最核心的心智模型，每次操作前先想清楚：我要把修改从哪个区移到哪个区。

### git restore

- `git restore <file>` —— 用暂存区内容覆盖工作区，丢弃本地修改

### 分支实验区

这是 experiment 分支上的实验内容，不会影响 master 分支。

### 分支操作

- `git branch` —— 查看所有分支
- `git switch -c <name>` —— 创建并切换到新分支
- `git merge <branch>` —— 将指定分支合并到当前分支
- `git branch -d <name>` —— 删除分支

### 合并类型

- **快进合并（Fast-Forward）**：目标分支无新提交，直接移动指针
- **三方合并（Three-Way Merge）**：两个分支都有新提交，生成合并提交

### 今日学习用时

约 45 分钟

### 分支使用建议

日常开发中，建议为每个独立功能创建分支，完成后合并回主分支。

### 今日待办

- [ ] 复习 git restore
- [ ] 练习分支切换
- [ ] 写一篇学习总结

### 学习进度

已完成 13 个动手练习，覆盖了 Git 核心操作。

### 远程协作笔记

这是在"另一台电脑"上添加的内容，通过 push → pull 同步回主仓库。

---

## 2026-05-10 完整学习总结

### 13 个动手练习

| # | 命令 | 学到什么 |
|---|---|---|
| 1 | init / add / commit | Git 三区域模型（工作区 → 暂存区 → 仓库） |
| 2 | diff | 工作区 vs 暂存区的内容差异 |
| 3 | restore / restore --staged | 丢弃工作区修改 / 取消暂存 |
| 4 | switch -c | 创建分支，工作区内容随分支切换变化 |
| 5 | merge（快进） | 目标分支无新提交时只移动指针 |
| 6 | merge（三方 + 冲突） | 双方都有新提交，手动解决冲突标记 |
| 7 | stash / reset | 暂存半成品 / 撤销错误提交 |
| 8 | reflog | 找回被 reset "删掉"的提交 |
| 9 | commit --amend | 修正最近一次提交的消息和内容 |
| 10 | rebase | 变基，保持线性历史 |
| 11 | revert | 创建反向提交来安全撤销 |
| 12 | remote add / push | 连接 GitHub 并推送 |
| 13 | clone / pull | 模拟协作：另一目录 clone → push → pull |

### 核心心智模型

```
工作区 ──git add──► 暂存区 ──git commit──► 本地仓库 ──git push──► 远程仓库
  │                    │                       │                      │
  └── git restore ────►│                       │                      │
  │                    │                       │                      │
  └── git diff ───────►│                       │                      │
                       │                       │                      │
              git restore --staged             │                      │
                       │                       │                      │
              git reset ◄──────────────────────┘                      │
                       │                                              │
              git pull ◄──────────────────────────────────────────────┘
```

### 分支工作流

```
master ●────●────●────────●  ← 主线
            \             \
feature      ●────●        ← 分支开发，merge 或 rebase 回主线
```

### 撤销操作速查

| 场景 | 命令 | 安全性 |
|---|---|---|
| 改错了想丢弃 | `git restore <file>` | ⚠️ 不可逆 |
| add 错了想取消 | `git restore --staged <file>` | ✅ 安全 |
| 改到一半切分支 | `git stash` → `git stash pop` | ✅ 安全 |
| 提交错了（未推送） | `git reset HEAD~1` | ⚠️ 本地安全 |
| 提交错了（已推送） | `git revert <commit>` | ✅ 安全 |
| 丢了提交想找回 | `git reflog` → `git reset --hard <hash>` | ✅ 可救回 |
| 修正最近提交 | `git commit --amend` | ⚠️ 仅限未推送 |

### 远程协作

```
本地 ──git push──► GitHub ──git pull──► 另一台电脑
                    ▲
              origin/master
```

- `git remote add origin <url>` —— 关联远程（一次性）
- `git push -u origin master` —— 首次推送并建立追踪
- `git push` —— 之后直接推送
- `git pull` —— 拉取远程更新
- `git clone <url>` —— 完整复制仓库
